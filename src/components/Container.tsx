import React from 'react'
import {ContaierProps} from './props.types'

const Container = (props:ContaierProps) => {
    return (
        <div style={props?.style}>
            
        </div>
    )
}

export default Container
